<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap');
        :root { --bg: #000; --surface: #111; --primary: #0071e3; --text: #f5f5f7; }
        body { background: var(--bg); color: var(--text); font-family: 'Plus Jakarta Sans', sans-serif; margin: 0; padding: 0; }
        .nav { backdrop-filter: blur(20px); background: rgba(0,0,0,0.8); border-bottom: 1px solid #333; padding: 20px 5%; display: flex; justify-content: space-between; position: sticky; top: 0; z-index: 100; }
        .logo { font-size: 24px; font-weight: 800; letter-spacing: -1px; }
        .hero { text-align: center; padding: 100px 20px; background: radial-gradient(circle at center, #1a1a1a 0%, #000 100%); }
        .hero h1 { font-size: 64px; font-weight: 800; margin: 0; background: linear-gradient(180deg, #fff, #888); -webkit-background-clip: text; -webkit-text-fill-color: transparent; letter-spacing: -2px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; max-width: 1200px; margin: -40px auto 50px auto; padding: 0 20px; }
        .card { background: var(--surface); border-radius: 24px; padding: 20px; border: 1px solid #222; transition: 0.4s; text-align: center; }
        .card:hover { transform: translateY(-10px); border-color: var(--primary); box-shadow: 0 20px 40px rgba(0,113,227,0.15); }
        .card img { width: 100%; height: 250px; object-fit: cover; border-radius: 16px; margin-bottom: 20px; }
        .btn { background: var(--primary); color: #fff; padding: 15px 30px; border-radius: 30px; border: none; font-size: 16px; font-weight: 600; cursor: pointer; transition: 0.3s; width: 100%; }
        .btn:hover { background: #005bb5; transform: scale(0.98); }
    </style>
</head>
<body>
    <div class="nav">
        <div class="logo">HAMZA GSM</div>
    </div>
    
    <div class="hero">
        <h1>Gelecek. Avucunuzda.</h1>
        <p style="color:#888; font-size:20px;">Premium cihazlar, profesyonel güvence.</p>
    </div>

    <div class="grid">
        <% products.forEach(function(p) { %>
            <div class="card">
                <img src="<%= p.img %>" alt="<%= p.model %>">
                <h2 style="margin:0 0 10px 0; font-size:22px;"><%= p.marka %> <%= p.model %></h2>
                <div style="font-size: 28px; font-weight: 800; margin-bottom: 20px;"><%= p.fiyat.toLocaleString('tr-TR') %> ₺</div>
                <button class="btn" onclick="alert('Ödeme modülü bir sonraki adımda eklenecek!')">Güvenli Satın Al</button>
            </div>
        <% }); %>
    </div>
</body>
</html>